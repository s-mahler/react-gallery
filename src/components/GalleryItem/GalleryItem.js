import React, { Component } from 'react';

class GalleryItem extends Component {
    render() {
        return (
           <>
            {/* <img key={this.props.image.id} src={this.props.image.path} alt={this.props.image.description} width="100" height="100"/> */}
           </> 
        )
    }
}

export default GalleryItem;